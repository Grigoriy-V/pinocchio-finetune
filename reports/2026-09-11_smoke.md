# Smoke run: 5 steps on an L40S, 2026-09-11

`modal run modal_apps/train_app.py::train --set v1 --run smoke --max-steps 5`
after the fourth `tokenize` (782 samples). Modal app
`ap-LSopFO64pHKGEYCCiLAoIW`; the record is `/vol/runs/smoke/train.json`.

| | |
|---|---|
| stack | torch 2.8.0, transformers 5.14.1, TRL/PEFT/bitsandbytes at their floors — loaded and trained, no version conflict |
| model class | `AutoModelForImageTextToText`, NF4 double-quant, bf16 compute, sdpa |
| LoRA | r=16 α=32 on `language_model` q/k/v/o/gate/up/down: **65,568,768 trainable parameters** |
| data | 782 samples; TRL dropped none as fully masked |
| step | 8 samples (batch 1 × accumulation 8), ~41k tokens |
| speed | **55 s per step**, ≈750 tokens/s |
| memory | **peak 12.23 GiB** |
| loss | 1.228 at step 5, mean target-token accuracy 0.828, grad norm 12.1, lr still in warmup (2.9e-5) |
| wall | 300 s in the function, 274 s of them training |

What it settles: the four open risks of the plan (versions, model
class, template shapes, and the fit) are closed — the run trains. What
it changes: the plan's 1–1.5 h estimate assumed 1.5–2.5k tokens/s; the
L40S does 750 with a 4-bit base and checkpointing, so the full run of
196 steps is ~3 h and ~$6 there, not $2–3. The peak of 12 GiB says an
A10 (24 GB, $1.10/h) fits but is slower still; a faster card is the
cheaper one per run when the price per hour rises less than the speed.

Options for the full run, estimates from the smoke's tokens/s scaled by
the cards' bf16 throughput — a guess until measured:

| GPU | $/h | est. speed | est. time | est. cost |
|---|---|---|---|---|
| L40S | 1.95 | 750 tok/s (measured) | 3.0 h | $5.9 |
| A100-80 | 2.50 | ~1.3k | 1.7 h | $4.3 |
| H100 | 3.95 | ~2–2.5k | 1.0–1.2 h | $4–5 |

`TUNE_GPU=<name>` chooses the card per run and the run record keeps it.
